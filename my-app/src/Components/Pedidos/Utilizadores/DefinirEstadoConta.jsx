import usePatch from "../HTTPTest/PATCH";
import React from "react";

function DefinirEstadoConta(props) {
  const Data = props.Data;
  const token = JSON.parse(localStorage.getItem("Token"));
  const FORM_ENDPOINT = "";
}
